/**
 * Constant values used throughout the project.
 */
export const CONSTANTS = {
    MAX_TOKEN_TRANSFER: 1000000, // Maximum tokens that can be transferred in a single transaction
    MIN_VOTE_THRESHOLD: 100,      // Minimum votes required for a proposal to be considered
    DEFAULT_GAS_LIMIT: 2000000,   // Default gas limit for transactions
    SUPPORTED_CHAINS: ['Ethereum', 'Binance Smart Chain'], // Supported blockchain networks
};
